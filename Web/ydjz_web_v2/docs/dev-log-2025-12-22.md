# 开发日志 - 2025年12月22日

## 今日开发概述

开始 **PC 端（Desktop）** 开发，完成了顶部导航栏、总览页面、明细页面的基础布局和功能。

---

## 今日开发内容

### 1. 顶部导航栏优化 (HomeLayout.vue)

#### 悬浮毛玻璃效果

顶部导航栏改为 `position: fixed`，滚动时触发毛玻璃效果：

```typescript
const isScrolled = ref(false)

function handleScroll() {
  isScrolled.value = window.scrollY > 20
}

onMounted(() => {
  window.addEventListener('scroll', handleScroll, { passive: true })
})
```

```css
.top-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  padding: 16px 32px;
  transition: background 0.3s, backdrop-filter 0.3s, box-shadow 0.3s;
}

.top-header.is-scrolled {
  background: rgba(255, 255, 255, 0.75);
  backdrop-filter: blur(20px);
  box-shadow: 0 2px 16px rgba(0, 0, 0, 0.06);
}

/* 暗色模式 */
html.dark .top-header.is-scrolled {
  background: rgba(20, 20, 20, 0.75);
  box-shadow: 0 2px 16px rgba(0, 0, 0, 0.3);
}
```

---

### 2. 总览页面重构 (Board.vue)

#### 左右双栏布局

```
┌─────────────────────┬─────────────────────┐
│     总资产卡片       │      (空白)          │
├─────────────────────┼─────────────────────┤
│     年度选择         │     年度趋势图表     │
├─────────────────────┼─────────────────────┤
│   月度概览(list/grid)│     账户明细列表     │
└─────────────────────┴─────────────────────┘
```

```css
.board-layout {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}
```

#### 月度概览双模式

支持 list/grid 切换，与移动端保持一致：

```typescript
const layoutMode = ref<'list' | 'grid'>('list')

function toggleLayout() {
  layoutMode.value = layoutMode.value === 'list' ? 'grid' : 'list'
}
```

```vue
<el-button
  :icon="layoutMode === 'list' ? Grid : List"
  circle
  size="small"
  @click="toggleLayout"
/>
```

#### 账户明细

直接显示在页面右下方，无需抽屉：

```vue
<div class="account-section">
  <h2 class="section-title">账户明细</h2>
  <div class="account-list">
    <div class="account-item" v-for="acc in accountList" :key="acc.id">
      <div class="account-info">
        <div class="account-name">{{ acc.accountName }}</div>
        <div class="account-note" v-if="acc.note">{{ acc.note }}</div>
      </div>
      <div class="account-amount">
        <div class="account-asset">¥ {{ acc.accountAsset }}</div>
        <div class="account-real" v-if="acc.realAsset">净 ¥ {{ acc.realAsset }}</div>
      </div>
    </div>
  </div>
</div>
```

---

### 3. 明细页面开发 (Flow.vue)

#### 左右布局（列表在左，操作在右）

```
┌────────────────────────────────────┬──────────────┐
│   日期 | 分类 | 账户 | 备注 | 金额 | 操作  │   月份选择   │
│  ─────────────────────────────────  ├──────────────┤
│   流水列表（可滚动）                 │  收入/支出   │
│                                    │    结余      │
│                                    ├──────────────┤
│                                    │   筛选条件   │
│                                    ├──────────────┤
│                                    │   导出报表   │
└────────────────────────────────────┴──────────────┘
```

```css
.flow-layout {
  display: grid;
  grid-template-columns: 1fr 280px;
  gap: 20px;
  flex: 1;
  min-height: 0;
}
```

#### 固定高度 + 中间滚动

页面不整体滚动，只有流水列表区域滚动：

```css
.flow-page {
  padding: 20px 20px 12px 20px;
  height: calc(100vh - 80px);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.left-panel {
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.list-header {
  flex-shrink: 0;
}

.flow-list-wrapper {
  flex: 1;
  overflow-y: auto;
}
```

#### 筛选功能

```vue
<div class="filter-card">
  <div class="filter-group">
    <label class="filter-label">类型</label>
    <el-radio-group v-model="handleType" size="small" @change="onFilterChange">
      <el-radio-button :value="3">全部</el-radio-button>
      <el-radio-button :value="0">收入</el-radio-button>
      <el-radio-button :value="1">支出</el-radio-button>
      <el-radio-button :value="2">转账</el-radio-button>
    </el-radio-group>
  </div>
  <div class="filter-group">
    <label class="filter-label">排序</label>
    <el-radio-group v-model="orderType" size="small" @change="onFilterChange">
      <el-radio-button :value="0">按时间</el-radio-button>
      <el-radio-button :value="1">按金额</el-radio-button>
    </el-radio-group>
  </div>
</div>
```

---

### 4. FlowItem 组件 (FlowItem.vue)

PC 端流水项组件，使用 Grid 布局对齐表头：

```css
.flow-item {
  display: grid;
  grid-template-columns: 60px 180px 200px 1fr 120px 80px;
  align-items: center;
  gap: 16px;
  padding: 12px 16px;
  border-bottom: 1px solid var(--color-border);
  cursor: pointer;
}

.flow-item:hover {
  background: var(--color-bg-page);
}
```

#### 操作按钮 hover 显示

```css
.flow-actions {
  display: flex;
  gap: 8px;
  opacity: 0;
  transition: opacity 0.2s;
}

.flow-item:hover .flow-actions {
  opacity: 1;
}
```

#### 账户图标

复用移动端的账户图标：

```typescript
import alipayIcon from '@shared/assets/icons/alipay.svg'
import wechatIcon from '@shared/assets/icons/wechat.svg'
import housingFundIcon from '@shared/assets/icons/housing-fund.svg'
import cashIcon from '@shared/assets/icons/cash.svg'

function getAccountIcon(name: string | undefined): string | null {
  if (!name) return null
  if (name.includes('支付宝')) return alipayIcon
  if (name.includes('微信')) return wechatIcon
  if (name.includes('公积金')) return housingFundIcon
  if (name.includes('现金')) return cashIcon
  return null
}
```

---

### 5. 720p 最小分辨率

新增 Desktop 基础样式文件，限制最小分辨率为 720p (1280x720)：

**src/desktop/styles/base.css:**
```css
html, body {
  min-width: 1280px;
  min-height: 720px;
}

#app {
  min-width: 1280px;
  min-height: 720px;
}
```

**main-desktop.ts:**
```typescript
import '@desktop/styles/base.css'
```

---

## 新增文件清单

| 文件路径 | 说明 |
|---------|------|
| `src/desktop/views/Board.vue` | PC 端总览页面 |
| `src/desktop/views/Flow.vue` | PC 端明细页面 |
| `src/desktop/components/FlowItem.vue` | PC 端流水项组件 |
| `src/desktop/styles/base.css` | PC 端基础样式（最小分辨率） |
| `docs/dev-log-2025-12-22.md` | 开发日志 |

## 修改文件清单

| 文件路径 | 修改内容 |
|---------|---------|
| `src/desktop/layouts/HomeLayout.vue` | 悬浮毛玻璃效果、fixed 定位 |
| `src/main-desktop.ts` | 引入 base.css |

---

## PC 端开发进度

| 页面 | 状态 | 说明 |
|-----|------|------|
| Auth 登录页 | ✅ 完成 | 登录/注册功能 |
| HomeLayout 导航 | ✅ 完成 | 顶部胶囊导航 + 毛玻璃 |
| Board 总览 | ✅ 完成 | 双栏布局、月度概览、账户明细 |
| Flow 明细 | ✅ 完成 | 流水列表、筛选、中间滚动 |
| Analysis 统计 | 🔲 待开发 | - |
| Setting 设置 | ✅ 完成 | 主题切换、关于弹窗 |

---

## 待开发功能

- 年度趋势图表（ECharts）
- 统计页面（Analysis.vue）
- 流水添加/编辑弹窗
- 数据管理子页面（收支管理、账户管理、分类管理、快记模板）
