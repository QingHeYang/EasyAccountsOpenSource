# 开发日志 - 2025年12月21日

## 今日开发概述

解决了**弹窗层级问题**，完善了**统计页面**（网格布局+分类明细），优化了**背景装饰**和**账单新增**页面的交互体验。

---

## 今日开发内容

### 1. 弹窗层级问题修复

#### 问题描述
大胶囊 Tabbar 使用 `backdrop-filter` 导致 GPU 合成层，遮挡了 `van-popup` 和 `van-action-sheet` 弹窗。

#### 根本原因
`van-config-provider` 改变了 Vant 组件的默认 teleport 目标，导致弹窗渲染在 config-provider 内部而非 body。

#### 解决方案
给所有 `van-popup` 和 `van-action-sheet` 添加 `teleport="body"`：

```vue
<van-popup v-model:show="showPopup" teleport="body">
<van-action-sheet v-model:show="showSheet" teleport="body">
```

#### 修改文件
- `Screen.vue`
- `FlowAdd.vue`
- `Account.vue`
- `TypeAdd.vue`
- `TemplateAdd.vue`
- `TemplateTag.vue`
- `Setting.vue`
- `Analysis.vue`

#### base.css z-index 调整
```css
.van-overlay { z-index: 9998 !important; }
.van-popup { z-index: 9999 !important; }
.van-action-sheet { z-index: 9999 !important; }
```

---

### 2. HomeLayout 背景装饰

将 Auth.vue 的背景装饰（银行卡形状 + 蓝色线条）应用到 HomeLayout，让所有主页面共享统一背景。

```vue
<div class="bg-decoration">
  <!-- 红色银行卡（右上角） -->
  <div class="card-shape card-red"></div>
  <!-- 绿色银行卡（左下角） -->
  <div class="card-shape card-green"></div>
  <!-- 蓝色三条线 -->
  <div class="blue-lines">
    <div class="line line-1"></div>
    <div class="line line-2"></div>
    <div class="line line-3"></div>
  </div>
</div>
```

```css
.card-shape {
  position: absolute;
  width: 360px;
  height: 220px;
  border-radius: 20px;
  opacity: 0.12;
}

.card-red {
  background: linear-gradient(135deg, #ff6b6b 0%, #ee5a5a 100%);
  top: -80px;
  right: -120px;
  transform: rotate(-15deg);
}

.card-green {
  background: linear-gradient(135deg, #51cf66 0%, #40c057 100%);
  bottom: 60px;
  left: -160px;
  transform: rotate(25deg);
}

.blue-lines .line {
  position: absolute;
  height: 2px;
  background: linear-gradient(90deg, transparent 0%, #74c0fc 50%, transparent 100%);
  opacity: 0.3;
}
```

同时将各页面背景改为 `transparent` 以显示装饰：
- `Analysis.vue`
- `Board.vue`
- 等

---

### 3. 统计页面日期选择器重构 (Analysis.vue)

#### 问题
原设计是双层弹窗嵌套（筛选弹窗内嵌日期选择器弹窗），用户体验差。

#### 解决方案
使用 `van-picker-group` 配合 tabs，在独立弹窗中选择开始/结束月份：

```vue
<van-popup v-model:show="showDatePicker" position="bottom" round teleport="body">
  <van-picker-group
    title="选择时间范围"
    :tabs="['开始月份', '结束月份']"
    @confirm="onDatePickerConfirm"
    @cancel="showDatePicker = false"
  >
    <van-date-picker
      v-model="chooseStartTime"
      :min-date="minDate"
      :max-date="maxDate"
      :columns-type="['year', 'month']"
    />
    <van-date-picker
      v-model="chooseEndTime"
      :min-date="endMinDate"
      :max-date="maxDate"
      :columns-type="['year', 'month']"
    />
  </van-picker-group>
</van-popup>
```

#### 结束日期限制
```typescript
const endMinDate = computed(() => {
  if (chooseStartTime.value.length === 2) {
    const year = parseInt(chooseStartTime.value[0])
    const month = parseInt(chooseStartTime.value[1]) - 1
    return new Date(year, month, 1)
  }
  return minDate
})
```

---

### 4. 统计页面网格布局 (Analysis.vue)

#### 4.1 分类列表改为 2 列网格

```vue
<div class="type-grid">
  <div
    v-for="item in currentTypeList"
    :key="item.id"
    class="type-card"
    :class="{ disabled: item.disabled }"
    @click="toTypeDetail(item.id)"
    @contextmenu.prevent="toggleTypeDisabled(item.id)"
  >
    <div class="type-name">{{ item.name }}</div>
    <div class="card-footer">
      <span class="card-money">¥{{ item.money }}</span>
      <span class="type-percent" :class="tabIndex === 0 ? 'income' : 'expense'">
        {{ item.percent }}%
      </span>
    </div>
  </div>
</div>
```

```css
.type-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
  padding: 0 16px;
}
```

#### 4.2 百分比突出显示

百分比使用主题色+加粗，金额使用次要灰色：

```css
.card-money {
  font-size: 14px;
  color: var(--color-text-secondary);
}

.type-percent {
  font-size: 16px;
  font-weight: 600;
}

.type-percent.income { color: var(--color-income); }
.type-percent.expense { color: var(--color-expense); }
```

#### 4.3 金额简化显示

顶部收入/支出总额，上万时自动简化：

```typescript
function formatLargeAmount(amount: string): string {
  const num = parseFloat(amount)
  if (isNaN(num)) return '0.00'
  if (Math.abs(num) >= 10000) {
    return (num / 10000).toFixed(2) + '万'
  }
  return amount
}
```

#### 4.4 点击切换金额显示模式

点击已选中的 Tab 切换缩写/完整金额：

```typescript
const showFullAmount = ref(false)

function onTabClick(index: number) {
  if (tabIndex.value === index) {
    showFullAmount.value = !showFullAmount.value
  } else {
    tabIndex.value = index
  }
}
```

#### 4.5 分类禁用功能

长按卡片禁用分类，前端重新计算百分比：

```typescript
const disabledTypeIds = ref<Set<number>>(new Set())

const currentTypeList = computed(() => {
  const list = tabIndex.value === 0 ? allInTypeList.value : allOutTypeList.value

  // 计算启用项的总金额
  const enabledTotal = list
    .filter(item => !disabledTypeIds.value.has(item.id))
    .reduce((sum, item) => sum + parseFloat(item.money || '0'), 0)

  // 重新计算百分比
  return list.map(item => {
    const isDisabled = disabledTypeIds.value.has(item.id)
    const money = parseFloat(item.money || '0')
    const newPercent = isDisabled || enabledTotal === 0
      ? 0
      : Math.round((money / enabledTotal) * 100)

    return { ...item, percent: newPercent, disabled: isDisabled }
  })
})
```

---

### 5. 分类明细页面 (AnalysisType.vue) - 新增

从统计页面点击某个分类，进入分类明细页面，展示该分类的月度统计。

#### 功能特性
- **分类选择器**: 两栏布局，可切换查看不同分类
- **快捷时间**: 近一年 / 今年 / 上年 / 自定义
- **年度统计卡片**: 展示每年的总收入/支出，可展开查看月度明细
- **月度流水弹窗**: 点击某月查看该月所有流水

#### 年度统计卡片
```vue
<div
  v-for="year in result.yearData"
  :key="year.year"
  class="year-card"
>
  <div class="year-header" @click="toggleYearExpand(year.year)">
    <span class="year-label">{{ year.year }}年</span>
    <div class="year-amounts">
      <span class="year-income">+{{ formatAmount(year.income) }}</span>
      <span class="year-outcome">-{{ formatAmount(year.outcome) }}</span>
    </div>
  </div>
  <!-- 月度明细 -->
  <div v-if="isYearExpanded(year.year)" class="month-grid">
    <div
      v-for="month in year.monthData"
      :key="month.month"
      class="month-item"
      @click="openFlowPopup(year.year, month.month)"
    >
      <span class="month-label">{{ month.month }}月</span>
      <span class="month-income">+{{ month.income }}</span>
      <span class="month-outcome">-{{ month.outcome }}</span>
    </div>
  </div>
</div>
```

---

### 6. 分析 API 封装 (analysis.ts) - 新增

```typescript
export interface AnalysisTypeItem {
  id: number
  parentId: number
  name: string
  money: string
  percent: number
}

export interface AnalysisTypeListResult {
  totalIn: string
  totalOut: string
  showInTypeList: AnalysisTypeItem[]
  showOutTypeList: AnalysisTypeItem[]
  allInTypeList: AnalysisTypeItem[]
  allOutTypeList: AnalysisTypeItem[]
}

export interface MonthData {
  month: number
  income: string
  outcome: string
}

export interface YearData {
  year: number
  income: string
  outcome: string
  monthData: MonthData[]
}

export const analysisApi = {
  getTypeList(params: AnalysisTypeListParams) {
    return getRequest().post<ApiResponse<AnalysisTypeListResult>>(
      '/analysis/v2/getAnalysisTypeList',
      params
    )
  },

  getTypeMonthData(params: AnalysisTypeMonthParams) {
    return getRequest().post<ApiResponse<AnalysisTypeMonthResult>>(
      '/analysis/v2/getAnalysisTypeMonthData',
      params
    )
  },
}
```

---

### 7. ECharts 工具 (echarts.ts) - 新增

按需引入 ECharts 组件，减少打包体积：

```typescript
import * as echarts from 'echarts/core'
import { PieChart, BarChart } from 'echarts/charts'
import { TitleComponent, TooltipComponent, LegendComponent } from 'echarts/components'
import { CanvasRenderer } from 'echarts/renderers'

echarts.use([
  PieChart,
  BarChart,
  TitleComponent,
  TooltipComponent,
  LegendComponent,
  CanvasRenderer
])

export default echarts
```

---

### 8. 账单新增页面改进 (FlowAdd.vue)

#### 8.1 收支选择器颜色区分

选中项根据 handle 显示对应颜色：

```vue
<div
  class="sheet-item"
  :class="{
    active: selectedAction?.id === action.id,
    [getActionClass(action.handle)]: selectedAction?.id === action.id
  }"
>
```

```css
.sheet-item.active { background: var(--color-transfer); }
.sheet-item.active.income { background: var(--color-income); }
.sheet-item.active.expense { background: var(--color-expense); }
.sheet-item.active.transfer { background: var(--color-transfer); }
```

#### 8.2 快记模板数据隔离

选择模板时先清空表单再填充，避免残留：

```typescript
function onSelectTemplate(template: Template) {
  // 先清空表单（保留备注和照片）
  money.value = ''
  selectedAction.value = null
  selectedAccount.value = null
  selectedAccountTo.value = null
  selectedType.value = null
  cascaderValue.value = ''
  chooseDate.value = formatDate(new Date())
  isCollect.value = false
  types.value = []

  // 再用模板数据填充
  if (template.money) money.value = template.money
  if (template.action) { /* ... */ }
  if (template.account) { /* ... */ }
  // ...
}
```

---

## 新增文件清单

| 文件路径 | 说明 | 行数 |
|---------|------|------|
| `src/mobile/views/analysis/AnalysisType.vue` | 分类明细页面 | ~1023 |
| `src/shared/api/analysis.ts` | 分析 API 封装 | ~92 |
| `src/shared/utils/echarts.ts` | ECharts 按需引入 | ~53 |
| `docs/dev-log-2025-12-21.md` | 开发日志 | - |

## 修改文件清单

| 文件路径 | 修改内容 |
|---------|---------|
| `src/mobile/layouts/HomeLayout.vue` | 背景装饰（银行卡+蓝线）、毛玻璃优化 |
| `src/mobile/styles/base.css` | z-index 调整 |
| `src/mobile/views/Analysis.vue` | 网格布局、日期选择器重构、禁用功能 |
| `src/mobile/views/flow/FlowAdd.vue` | 收支颜色、模板数据隔离 |
| `src/mobile/views/Screen.vue` | teleport 修复 |
| `src/mobile/views/Setting.vue` | teleport 修复 |
| `src/mobile/views/setting/Account.vue` | teleport 修复 |
| `src/mobile/views/setting/TypeAdd.vue` | teleport 修复 |
| `src/mobile/views/setting/template/TemplateAdd.vue` | teleport 修复 |
| `src/mobile/views/setting/template/TemplateTag.vue` | teleport 修复 |
| `src/mobile/router.ts` | 添加 AnalysisType 路由 |
| `src/shared/api/index.ts` | 导出 analysisApi |
| `package.json` | 添加 echarts 依赖 |

---

## 交互说明

### 统计页面 (Analysis)

| 操作 | 效果 |
|-----|------|
| 点击收入/支出 Tab | 切换查看收入/支出分类 |
| 再次点击已选中 Tab | 切换金额显示模式（缩写/完整） |
| 点击分类卡片 | 跳转到该分类明细页 |
| 长按分类卡片 | 禁用/启用该分类，其他分类重算百分比 |

### 分类明细页 (AnalysisType)

| 操作 | 效果 |
|-----|------|
| 点击顶部分类名 | 打开分类选择器 |
| 点击快捷时间 | 切换时间范围 |
| 点击年度卡片 | 展开/收起月度明细 |
| 再次点击年度金额 | 切换金额显示模式 |
| 点击月度项 | 查看该月流水列表 |

---

## 路由配置更新

```typescript
/analysis/type   # 分类明细页（带 query: typeId, start, end）
```

---

---

### 9. 图表组件开发

#### 9.1 ChartOverlay - 首页年度图表

首页 Board 页面的全屏横向图表组件，包含趋势折线图和账户柱状图。

**特性：**
- 竖屏设备自动旋转90度显示横屏效果
- 胶囊样式 Tab 切换（趋势/账户）
- 滑动指示器动画

```vue
<ChartOverlay
  v-model:show="showChartOverlay"
  :year="chooseYear"
  :month-details="homeInfo.monthDetails"
  :accounts="homeInfo.accounts"
/>
```

```css
.chart-container {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100vh;
  height: 100vw;
  transform: translate(-50%, -50%) rotate(90deg);
}

@media (orientation: landscape) {
  .chart-container {
    width: 100vw;
    height: 100vh;
    transform: translate(-50%, -50%);
  }
}
```

#### 9.2 AnalysisChartOverlay - 统计页分类饼图

统计页面的全屏横向饼图组件，显示收入/支出分类占比。

**特性：**
- 红绿胶囊切换（收入绿色/支出红色）
- 左侧饼图带标签线
- 右侧信息面板（标题、总金额、分类列表）
- 点击分类可禁用，饼图实时更新

```vue
<AnalysisChartOverlay
  v-model:show="showChartOverlay"
  :total-in="totalIn"
  :total-out="totalOut"
  :in-type-list="allInTypeList"
  :out-type-list="allOutTypeList"
/>
```

#### 9.3 YearLineChartOverlay - 分类明细年度折线图

分类明细页的年度数据折线图，显示该分类某一年的月度收支趋势。

**特性：**
- 收入/支出双折线，带渐变面积
- 右侧信息面板显示年份和分类名
- 点击年份 header 的图表按钮打开

```vue
<YearLineChartOverlay
  v-model:show="showYearChart"
  :year="chartYear"
  :type-name="selectedTypeName"
  :month-data="chartMonthData"
/>
```

---

### 10. ECharts 组件扩展

为支持图表功能，扩展了 ECharts 按需引入的组件：

```typescript
import {
  TitleComponent,
  TooltipComponent,
  LegendComponent,
  GridComponent,
  RadarComponent,
  DatasetComponent,
  GraphicComponent,  // 新增：自定义图形
} from 'echarts/components'

import { PieChart, BarChart, LineChart, RadarChart } from 'echarts/charts'
```

---

### 11. Board 页面最高值标签样式

月度概览列表中，最高收入/支出/结余使用标签样式突出显示：

```vue
<span class="stat-num" :class="{ 'max-tag income': isMaxIncome(item.month) }">
  +{{ formatMonthAmount(item.income) }}
</span>
```

```css
.max-tag {
  padding: 2px 6px;
  border-radius: 4px;
  color: #fff !important;
}

.max-tag.income { background: var(--color-income); }
.max-tag.expense { background: var(--color-expense); }
.max-tag.balance { background: var(--color-transfer); }
```

---

### 12. AI 网络配置（预置）

为后续 AI 功能预置了网络配置：

**vite.config.ts 代理配置：**
```typescript
'/ai-api': {
  target: 'http://127.0.0.1:10680',
  changeOrigin: true,
  rewrite: (path) => path.replace(/^\/ai-api/, ''),
}
```

**运行时配置 (public/config.js)：**
```javascript
window.__APP_CONFIG__ = {
  aiApiUrl: '/ai-api',
  aiWebSocketUrl: 'ws://127.0.0.1:10680/ws',
}
```

**新增文件：**
- `src/shared/api/ai-request.ts` - AI 专用 axios 实例
- `src/shared/api/ai.ts` - AI API 接口
- `src/shared/utils/ai-websocket.ts` - WebSocket 管理类

---

## 新增文件清单（更新）

| 文件路径 | 说明 |
|---------|------|
| `src/mobile/components/ChartOverlay.vue` | 首页年度图表组件 |
| `src/mobile/components/AnalysisChartOverlay.vue` | 统计页分类饼图组件 |
| `src/mobile/components/YearLineChartOverlay.vue` | 分类明细年度折线图组件 |
| `public/config.js` | 运行时配置 |
| `src/shared/api/ai-request.ts` | AI 请求封装 |
| `src/shared/api/ai.ts` | AI API 接口 |
| `src/shared/utils/ai-websocket.ts` | WebSocket 工具 |

## 修改文件清单（更新）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/mobile/views/Board.vue` | 图表按钮、最高值标签样式 |
| `src/mobile/views/Analysis.vue` | 饼图按钮、引入 AnalysisChartOverlay |
| `src/mobile/views/analysis/AnalysisType.vue` | 年度图表按钮、引入 YearLineChartOverlay |
| `src/shared/utils/echarts.ts` | 添加 GraphicComponent、LineChart |
| `src/shared/api/index.ts` | 导出 AI 相关模块 |
| `src/env.d.ts` | 添加 window.__APP_CONFIG__ 类型 |
| `vite.config.ts` | 添加 /ai-api 代理 |
| `index.html` | 引入 config.js |
| `mobile.html` | 引入 config.js |

---

## 备注

- 统计页面的禁用功能纯前端实现，刷新页面后重置
- 分类明细页支持从筛选页或统计页跳转
- 图表组件统一使用横屏布局，竖屏设备通过 CSS transform 旋转
- AI 网络配置已预置，待后续功能开发使用
