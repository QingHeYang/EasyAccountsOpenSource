# 开发日志 - 2025年12月29日

## 今日开发概述

完成 **AI+ 对话功能基础架构**、**AI+ UI 完善（Markdown/思维链/工具详情）**、**FlowList 月份选择器修复**、**FlowEditor 不计入统计分类折叠**、**TypeDetail 流水列表优化** 等功能。

---

## 文件变更统计

```
src/shared/services/chat/chatService.ts         | +250 行（新增）
src/shared/services/chat/messageStore.ts        | +600 行（新增）
src/shared/services/chat/UnifiedMessage.ts      | +167 行（新增）
src/shared/services/chat/index.ts               | +27 行（新增）
src/shared/utils/websocket.ts                   | +230 行（新增）
src/desktop/components/ai-plus/AIDrawer.vue     | +1086 行（重写完善）
src/desktop/components/ai-plus/AITriggerButton.vue | +65 行（新增）
src/desktop/components/ai-plus/AIBubble.vue     | +200 行（新增，备用）
src/desktop/components/ai-plus/index.ts         | +4 行（新增）
src/desktop/layouts/HomeLayout.vue              | +10 行变化
src/desktop/views/flow/FlowList.vue             | +84 行变化（月份选择器修复）
src/desktop/components/FlowItem.vue             | +9 行变化（showActions 属性）
src/desktop/components/flow/FlowEditor.vue      | +104 行变化（不计入分类折叠）
src/desktop/views/analysis/TypeDetail.vue       | +443 行变化（流水列表优化）
vite.config.ts                                  | -10 行变化
15+ files changed, ~2600 insertions(+)
```

---

## 今日开发内容

### 1. AI+ 对话功能基础架构

#### 1.1 Chat 服务架构

创建 `src/shared/services/chat/` 服务包，提供 WebSocket 通信和消息管理。

```
src/shared/services/chat/
├── index.ts           # 统一导出
├── chatService.ts     # WebSocket 连接管理
├── messageStore.ts    # 消息状态管理
└── UnifiedMessage.ts  # 统一消息数据结构
```

#### 1.2 UnifiedMessage 统一消息结构

支持多种消息类型：用户消息、AI 回复、工具调用、子智能体。

```typescript
export type MessageRole = 'user' | 'assistant' | 'tool' | 'agent'
export type MessageType = 'text' | 'tool' | 'agent'
export type ToolStatus = 'pending' | 'success' | 'error'

export class UnifiedMessage {
  id: string
  role: MessageRole | ''
  messageType: MessageType | ''
  content: MessageContent      // 文本内容
  tool: ToolInfo              // 工具调用信息
  agent: AgentInfo            // 子智能体信息
  meta: MessageMeta           // 元数据
}
```

#### 1.3 ChatService WebSocket 管理

管理 WebSocket 连接、重连、心跳响应。

**连接配置：**
```typescript
const DEFAULT_CONFIG: ChatConfig = {
  userId: 'user_67ce21d6-a11c-4340-851b-7a8949906aa3',
  appId: 'easy-accounts-agent',
  useThinkLlm: false
}
```

**WebSocket URL（直连后端）：**
```
ws://www.lllama.cn:10676/ws/chat?user_id=xxx&agent_id=xxx
```

**消息发送格式：**
```json
{
  "conversation_id": "会话ID",
  "content": "用户消息"
}
```

**Composable 接口：**
```typescript
export function useChatService() {
  return {
    connectionState,    // 连接状态：disconnected | connecting | connected | error
    isStreaming,        // 是否正在流式输出
    connect,            // 连接 WebSocket
    disconnect,         // 断开连接
    sendMessage,        // 发送消息
    newConversation,    // 新建对话
  }
}
```

#### 1.4 MessageStore 消息存储

响应式消息管理，处理 WebSocket 消息并维护消息列表。

**支持的消息类型：**

| 类型 | 说明 |
|-----|------|
| `chunk` | 流式文本片段 |
| `segment` | 完整段落 |
| `tool_call` | 工具调用开始 |
| `tool_response` | 工具调用结果 |
| `start` | 子智能体启动 |
| `exit` | 对话结束 |
| `title` | 标题更新 |
| `error` | 错误消息 |

#### 1.5 AI+ 组件包

```
src/desktop/components/ai-plus/
├── index.ts              # 统一导出
├── AIDrawer.vue          # AI 抽屉组件（主要使用）
├── AITriggerButton.vue   # 触发按钮（显示 AI+ 文字）
└── AIBubble.vue          # 气泡组件（备用方案）
```

#### 1.6 AIDrawer 抽屉组件

右侧固定抽屉，宽度 460px，支持完整对话功能。

**功能特性：**
- 抽屉打开时自动连接 WebSocket
- 连接状态显示（未连接/连接中/已连接/连接失败）
- 消息列表（用户/AI/工具/智能体）
- 多行输入框（1-6行，最多500字）
- 新对话按钮

**消息气泡样式（使用项目主色）：**
```css
.user-bubble {
  background: var(--color-transfer);  /* 蓝色 #1890FF */
}

.assistant-bubble {
  background: var(--color-bg-page);
}

.tool-bubble {
  background: var(--color-transfer-bg);
}
```

#### 1.7 HomeLayout 集成

```vue
<div class="content-wrapper" :class="{ 'drawer-open': aiDrawerOpen }">
  <main class="main-content">...</main>
  <AIDrawer v-model="aiDrawerOpen" />
</div>
<AITriggerButton :visible="!aiDrawerOpen" @click="aiDrawerOpen = true" />
```

---

### 2. AI+ UI 完善

重写 `AIDrawer.vue` 组件，添加完整的 UI 功能。

#### 2.1 新增依赖

```bash
pnpm add markdown-it
pnpm add -D @types/markdown-it
```

#### 2.2 Markdown 渲染

使用 `markdown-it` 渲染 AI 回复内容：

```typescript
import MarkdownIt from 'markdown-it'

const md = new MarkdownIt({
  html: true,
  linkify: true,
  typographer: true,
  breaks: true
})

function renderMarkdown(text: string): string {
  let html = md.render(text)
  // 为表格添加滚动容器
  html = html.replace(/<table([^>]*)>/g, '<div class="table-wrapper"><table$1>')
  html = html.replace(/<\/table>/g, '</table></div>')
  return html
}
```

**Markdown 样式支持：**
- 代码块 / 行内代码
- 表格（自动滚动）
- 列表（有序/无序）
- 引用块
- 链接

#### 2.3 思维链折叠/展开

AI 回复中的思考过程（reasoning）可折叠显示：

```typescript
// 每条消息独立记录展开状态
const reasoningExpanded = ref<Record<string, boolean>>({})

function toggleReasoning(msgId: string) {
  reasoningExpanded.value[msgId] = !reasoningExpanded.value[msgId]
}
```

```vue
<div v-if="msg.content.reasoning" class="reasoning-section">
  <div class="reasoning-header" @click="toggleReasoning(msg.id)">
    <el-icon :size="14">
      <component :is="isReasoningExpanded(msg.id) ? ArrowDown : ArrowRight" />
    </el-icon>
    <span class="reasoning-label">思考过程</span>
  </div>
  <div v-show="isReasoningExpanded(msg.id)" class="reasoning-content">
    {{ msg.content.reasoning }}
  </div>
</div>
```

#### 2.4 工具详情弹窗

点击工具调用气泡，打开详情弹窗：

```typescript
const toolDetailVisible = ref(false)
const currentToolDetail = ref<{
  name: string
  status: 'pending' | 'success' | 'error'
  arguments: Record<string, unknown>
  result: string
}>({ name: '', status: 'pending', arguments: {}, result: '' })

function showToolDetail(msg: UnifiedMessage) {
  currentToolDetail.value = {
    name: msg.tool.name,
    status: msg.tool.status,
    arguments: msg.tool.arguments,
    result: msg.tool.result
  }
  toolDetailVisible.value = true
}
```

**弹窗内容：**
- 执行状态（成功/失败/执行中）
- 调用参数（JSON 格式化，绿色终端风格）
- 返回结果（JSON 格式化）
- 一键复制按钮

#### 2.5 对话 ID 持久化

使用 localStorage 保存对话 ID，重新打开抽屉时恢复：

```typescript
const CONVERSATION_KEY = 'ai_conversation_id'

function saveConversationId(id: string) {
  localStorage.setItem(CONVERSATION_KEY, id)
}

function getSavedConversationId(): string | null {
  return localStorage.getItem(CONVERSATION_KEY)
}

// 连接时恢复对话
watch(() => props.modelValue, async (isOpen) => {
  if (isOpen && connectionState.value !== 'connected') {
    const savedId = getSavedConversationId()
    await connect(savedId || undefined)
  }
})
```

#### 2.6 连接状态增强

4 种状态显示，点击失败/未连接可重连：

```typescript
const connectionInfo = computed(() => {
  switch (connectionState.value) {
    case 'connecting':
      return { text: '连接中...', class: 'connecting', clickable: false }
    case 'connected':
      return { text: '已连接', class: 'connected', clickable: false }
    case 'error':
      return { text: '连接失败', class: 'error', clickable: true }
    default:
      return { text: '未连接', class: 'disconnected', clickable: true }
  }
})

async function handleReconnect() {
  disconnect()
  await connect()
  ElMessage.success('重连成功')
}
```

#### 2.7 其他功能

| 功能 | 说明 |
|-----|------|
| 消息时间显示 | 每条消息显示发送时间 |
| 复制 AI 回复 | hover 显示复制按钮 |
| 发送状态防抖 | 防止重复发送 |
| 新对话功能 | 清除当前对话，开启新会话 |
| 空状态提示 | 友好的空状态 UI |
| 流式光标 | 打字机效果光标闪烁 |

#### 2.8 暗色模式适配

使用项目主题变量，全面适配暗色模式：

```css
/* 气泡暗色模式 */
html.dark .assistant-bubble {
  background: var(--color-bg-container);
}

/* 思维链暗色模式 */
html.dark .reasoning-header {
  background: rgba(116, 192, 252, 0.1);
}

/* Markdown 暗色模式 */
html.dark .message-content :deep(code) {
  background: rgba(255, 255, 255, 0.1);
}

html.dark .message-content :deep(pre) {
  background: var(--color-bg-container);
}
```

---

### 4. FlowList 月份选择器修复

修复月份选择器显示两个日期的问题。

**问题：** 之前使用透明覆盖层方案，导致显示两个日期

**解决方案：** 直接使用 el-date-picker，通过 CSS 变量覆盖样式

```vue
<el-date-picker
  v-model="pickerMonth"
  type="month"
  format="YYYY年M月"
  class="month-picker"
/>
```

```css
.month-selector :deep(.month-picker) {
  --el-date-editor-width: auto;
  --el-fill-color-blank: transparent;
}

.month-selector :deep(.month-picker .el-input__wrapper) {
  background: transparent !important;
  box-shadow: none !important;
}

.month-selector :deep(.month-picker .el-input__inner) {
  color: #fff !important;
  font-size: 18px;
  font-weight: 600;
}
```

**暗色模式适配：**
```css
html.dark .flow-list-page .month-picker {
  --el-fill-color-blank: transparent;
}
```

---

### 5. FlowEditor 不计入统计分类折叠

将"不计入统计"的 action 分离，折叠隐藏。

```typescript
const normalActions = computed(() => actions.value.filter(a => !a.exempt))
const exemptActions = computed(() => actions.value.filter(a => a.exempt))
const exemptActionsExpanded = ref(false)
```

```vue
<!-- 普通 action -->
<div class="action-tabs">
  <div v-for="action in normalActions" ...>
    {{ action.hname }}
  </div>
</div>

<!-- 不计入的 action（折叠区域） -->
<div v-if="exemptActions.length > 0" class="exempt-actions-section">
  <div class="exempt-toggle" @click="exemptActionsExpanded = !exemptActionsExpanded">
    <span>不计入统计</span>
    <el-icon :class="{ rotated: exemptActionsExpanded }"><ArrowDown /></el-icon>
  </div>
  <div v-show="exemptActionsExpanded" class="exempt-actions">
    ...
  </div>
</div>
```

---

### 6. FlowItem showActions 属性

添加 `showActions` 属性，控制是否显示操作按钮（收藏/删除）。

```typescript
const props = withDefaults(defineProps<{
  flow: Flow
  showActions?: boolean
}>(), {
  showActions: true
})
```

```vue
<div v-if="showActions" class="flow-actions">
  ...
</div>
```

---

### 7. TypeDetail 流水列表优化

优化分类详情页的流水列表显示。

**改进内容：**
- 使用 FlowItem 组件显示流水（与 FlowList 保持一致）
- 按日期分组显示
- 支持收藏/删除操作
- 月份点击智能判断（只有收入显示收入，只有支出显示支出）

```typescript
// 按日期分组
const groupedFlows = computed(() => {
  const groups: { date: string; dateLabel: string; flows: Flow[] }[] = []
  // ... 分组逻辑
  return groups
})

// 月份点击逻辑
function onMonthItemClick(year: number, month: MonthData) {
  const hasIncome = parseFloat(month.income) > 0
  const hasOutcome = parseFloat(month.outcome) > 0

  if (hasIncome && !hasOutcome) {
    onMonthClick(year, month.month, 0) // 收入
  } else {
    onMonthClick(year, month.month, 1) // 支出（默认）
  }
}
```

---

## 文件变更清单

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/shared/services/chat/index.ts` | Chat 服务导出 |
| `src/shared/services/chat/chatService.ts` | WebSocket 连接管理 |
| `src/shared/services/chat/messageStore.ts` | 消息状态管理 |
| `src/shared/services/chat/UnifiedMessage.ts` | 统一消息结构 |
| `src/shared/utils/websocket.ts` | WebSocket 工具类（备用） |
| `src/desktop/components/ai-plus/index.ts` | 组件导出 |
| `src/desktop/components/ai-plus/AIDrawer.vue` | AI 抽屉组件 |
| `src/desktop/components/ai-plus/AITriggerButton.vue` | 触发按钮 |
| `src/desktop/components/ai-plus/AIBubble.vue` | 气泡组件（备用） |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/desktop/layouts/HomeLayout.vue` | 集成 AI 抽屉和触发按钮 |
| `src/desktop/views/flow/FlowList.vue` | 月份选择器样式修复 |
| `src/desktop/components/FlowItem.vue` | 添加 showActions 属性 |
| `src/desktop/components/flow/FlowEditor.vue` | 不计入统计分类折叠、z-index 调整 |
| `src/desktop/views/analysis/TypeDetail.vue` | FlowItem 组件、收藏/删除、分组显示 |
| `vite.config.ts` | 移除不需要的 WS 代理配置 |
| `package.json` | 添加 nanoid 依赖 |

---

## 技术要点

### WebSocket 直连（不走代理）

WebSocket 直接连接后端服务器，不通过 Vite 代理：

```typescript
const baseUrl = 'ws://www.lllama.cn:10676'
const wsUrl = `${baseUrl}/ws/chat?user_id=${userId}&agent_id=${agentId}`
```

### Element Plus CSS 变量覆盖

```css
.month-picker {
  --el-date-editor-width: auto;
  --el-fill-color-blank: transparent;
}
```

### 响应式消息管理

```typescript
class MessageStore {
  state = reactive({
    mainMessages: [],
    streamingMessageId: null,
  })
}
```

---

## 待开发功能（AI+ 后续任务）

### 高优先级

| 功能 | 说明 | 状态 |
|-----|------|------|
| 历史对话加载 | HTTP API 加载历史消息 | ❌ 待开发 |
| 对话标题更新 | 接收 title 消息后更新标题 | ✅ 已完成 |
| 消息 Markdown 渲染 | 使用 markdown-it 渲染 AI 回复 | ✅ 已完成 |
| 停止生成按钮 | 停止流式输出 | ❌ 待开发 |

### 中优先级

| 功能 | 说明 | 状态 |
|-----|------|------|
| 工具调用详情展示 | 点击展开工具参数和结果 | ✅ 已完成 |
| 思考过程折叠/展开 | reasoning 内容可折叠 | ✅ 已完成 |
| 对话列表侧边栏 | 历史对话列表 | ❌ 待开发 |
| 新建对话确认 | 清空当前对话前确认 | ❌ 待开发 |

### 低优先级

| 功能 | 说明 | 状态 |
|-----|------|------|
| 消息复制 | 复制 AI 回复内容 | ✅ 已完成 |
| 消息重新生成 | 重新生成上一条回复 | ❌ 待开发 |
| 输入框 @ 提及 | 快捷输入账户/分类 | ❌ 待开发 |
| 语音输入 | 语音转文字 | ❌ 待开发 |

---

## 当前 AI+ 功能状态

| 模块 | 状态 | 说明 |
|-----|------|------|
| WebSocket 连接 | ✅ 完成 | 直连后端，自动重连 |
| 消息发送 | ✅ 完成 | JSON 格式发送 |
| 消息接收 | ✅ 完成 | 支持 chunk/segment/tool/agent |
| 流式显示 | ✅ 完成 | 实时拼接显示 |
| 连接状态 | ✅ 完成 | 4 种状态显示 + 点击重连 |
| 抽屉 UI | ✅ 完成 | 480px 右侧抽屉 |
| 触发按钮 | ✅ 完成 | AI+ 文字按钮 |
| Markdown 渲染 | ✅ 完成 | markdown-it + 代码块/表格/列表 |
| 思维链折叠 | ✅ 完成 | 可折叠展开，每条消息独立状态 |
| 工具详情弹窗 | ✅ 完成 | 参数/结果 JSON 格式化显示 |
| 对话 ID 持久化 | ✅ 完成 | localStorage 存储 |
| 消息复制 | ✅ 完成 | hover 显示复制按钮 |
| 暗色模式 | ✅ 完成 | 全面适配主题变量 |
| 历史消息加载 | ❌ 待开发 | 需要 HTTP API |
| 停止生成按钮 | ❌ 待开发 | 服务层已有，UI 未接入 |

---

## PC 端开发进度

| 页面 | 状态 | 说明 |
|-----|------|------|
| Auth 登录页 | ✅ 完成 | 登录/注册功能 |
| HomeLayout 导航 | ✅ 完成 | 顶部胶囊导航 + AI 抽屉 |
| Board 总览 | ✅ 完成 | 年度趋势图表、月度概览 |
| Flow 明细 | ✅ 完成 | 列表、日历、筛选、月份选择器 |
| Analysis 统计 | ✅ 完成 | Tab 导航、分类详情、流水列表 |
| Setting 设置 | ✅ 完成 | 模块化拆分、样式统一 |
| AI+ 对话 | 🔄 进行中 | 基础架构完成，待完善 |
