# 开发日志 - 2026年1月6日

## 今日开发概述

**上午：** 实现 **移动端总览页隐私模式**，与桌面端保持一致。**AI+ 按钮重构**为浮动按钮。**全局错误拦截优化**，区分认证错误和业务错误，提升用户体验。

**下午：** **设置页面结构重构**，新增"系统管理"分类，创建"系统信息"功能（桌面端抽屉 + 移动端页面），整合外观设置、认证信息、备份信息、版本信息。简化"关于"对话框。

---

## 文件变更统计

```
src/shared/api/request.ts                                  | 修改（ApiError 类、错误拦截重构）
src/shared/api/index.ts                                    | 修改（导出新工具函数和类型）
src/shared/api/home.ts                                     | 修改（新增 AuthConfig、BackupConfig、SystemConfig）
src/main-desktop.ts                                        | 修改（错误提示优化）
src/main-mobile.ts                                         | 修改（错误提示优化）
src/mobile/views/Board.vue                                 | 修改（隐私模式 + AI浮动按钮）
src/desktop/views/settings/index.vue                       | 修改（页面结构重构）
src/desktop/views/settings/SystemInfo.vue                  | 新增（系统信息抽屉组件）
src/mobile/views/Setting.vue                               | 修改（页面结构重构、简化关于弹窗）
src/mobile/views/setting/SystemInfo.vue                    | 新增（系统信息页面）
src/mobile/router.ts                                       | 修改（新增路由）
11 files changed (2 new, 9 modified)
```

---

## 今日开发内容

### 1. 移动端总览页隐私模式

#### 1.1 功能需求

与桌面端一致，点击隐私按钮后：
- 所有金额显示为星号
- 不同区域星号数量不同
- 隐私模式下禁用账户按钮和图表按钮

#### 1.2 实现细节

**隐私常量定义：**

```typescript
const PRIVACY_TOTAL = '*******'   // 总资产 7个
const PRIVACY_ACCOUNT = '******'  // 账户明细 6个
const PRIVACY_YEAR = '******'     // 年度收支 6个
const PRIVACY_MONTH = '*****'     // 月度概览 5个
```

**隐私格式化函数：**

```typescript
function privacyTotal(amount: string | undefined): string {
  if (privacyMode.value) return PRIVACY_TOTAL
  return amount || '0.00'
}
// 类似的：privacyAccount, privacyYear, privacyMonth
```

**移动端特殊处理：**

与桌面端不同，移动端隐私模式下需要禁用交互：

```vue
<!-- 账户按钮禁用 -->
<div
  class="asset-action"
  :class="{ disabled: privacyMode }"
  @click="!privacyMode && (showAccountSheet = true)"
>

<!-- 图表按钮禁用 -->
<div
  class="action-btn"
  :class="{ disabled: privacyMode }"
  @click="!privacyMode && (showChartOverlay = true)"
>
```

**月度概览隐藏 +/- 符号：**

```vue
<span class="stat-num" :class="{ 'max-tag income': isMaxIncome(item.month) && !privacyMode }">
  <template v-if="!privacyMode">+</template>{{ privacyMonth(item.income) }}
</span>
```

---

### 2. AI+ 浮动按钮

#### 2.1 变更原因

AI+ 按钮从头部移至右下角浮动位置，与明细页新增按钮保持一致的交互体验。

#### 2.2 实现

**移除头部按钮：**

```vue
<!-- 删除 -->
<div v-if="aiServiceAvailable" class="header-action ai-btn" @click="toAI">
  <span class="ai-text">AI+</span>
</div>
```

**添加浮动按钮：**

```vue
<!-- 浮动 AI+ 按钮 -->
<div v-if="aiServiceAvailable" class="fab" @click="toAI">
  <span class="ai-fab-text">AI+</span>
</div>
```

**样式（与明细页一致）：**

```css
.fab {
  position: fixed;
  right: 20px;
  bottom: 100px;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-transfer);
  color: #fff;
  box-shadow: 0 4px 12px rgba(24, 144, 255, 0.4);
  z-index: 50;
}

.fab:active {
  transform: scale(0.95);
}
```

---

### 3. 全局错误拦截优化

#### 3.1 问题描述

之前的错误处理存在以下问题：

| 问题 | 表现 |
|------|------|
| 认证错误提示不友好 | 显示 "xxx失败" 而非 "登录已过期" |
| 错误码暴露给用户 | 显示 `42001: 余额不足` |
| 业务代码无法区分错误类型 | catch 中无法判断是认证错误还是业务错误 |

#### 3.2 解决方案

**新增 ApiError 类：**

```typescript
export class ApiError extends Error {
  code: number           // 错误码
  isAuthError: boolean   // 是否为认证错误
  handled: boolean       // 是否已全局处理

  constructor(code: number, message: string, isAuthError = false, handled = false) {
    super(message)
    this.name = 'ApiError'
    this.code = code
    this.isAuthError = isAuthError
    this.handled = handled
  }
}
```

**新增工具函数：**

```typescript
// 判断是否为认证错误
export function isAuthError(error: unknown): boolean {
  return error instanceof ApiError && error.isAuthError
}

// 判断是否已被全局处理
export function isHandledError(error: unknown): boolean {
  return error instanceof ApiError && error.handled
}
```

**响应拦截器重构：**

```typescript
// 认证错误码
const AUTH_ERROR_CODES = [401, 418, 4010, 4011]

instance.interceptors.response.use(
  (response) => {
    const { code, msg } = response.data

    // 成功
    if (code === 0) return response

    // 认证错误 - reject 并标记
    if (AUTH_ERROR_CODES.includes(code)) {
      if (code === 401 || code === 418) {
        localStorage.removeItem('token')
      }
      globalHandlers.onUnauthorized?.(code)
      return Promise.reject(new ApiError(code, '登录已过期，请重新登录', true, true))
    }

    // 业务错误 - 调用全局错误回调后 reject
    globalHandlers.onError?.(code, msg)
    return Promise.reject(new ApiError(code, msg, false, true))
  },
  (error) => {
    const status = error.response?.status
    const msg = error.response?.data?.msg || error.message || '网络错误'

    // HTTP 401/418
    if (status === 401 || status === 418) {
      localStorage.removeItem('token')
      globalHandlers.onUnauthorized?.(status)
      return Promise.reject(new ApiError(status, '登录已过期，请重新登录', true, true))
    }

    // 其他 HTTP 错误
    globalHandlers.onError?.(status || 0, msg)
    return Promise.reject(new ApiError(status || 0, msg, false, true))
  }
)
```

**错误提示优化：**

```typescript
// main-desktop.ts / main-mobile.ts
onError: (_code, msg) => {
  // 只显示消息，不显示错误码
  ElMessage.error(msg)  // 或 showNotify({ message: msg })
}
```

#### 3.3 业务代码用法

业务代码可以使用 `isAuthError()` 判断是否需要显示错误提示：

```typescript
import { isAuthError } from '@shared/api'

try {
  await flowApi.delete(id)
  showToast('删除成功')
} catch (err) {
  // 认证错误已由拦截器处理（跳转登录页），不需要显示"删除失败"
  if (!isAuthError(err)) {
    showToast('删除失败')
  }
}
```

---

## 文件变更清单

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/shared/api/request.ts` | 新增 ApiError 类、isAuthError/isHandledError 函数、响应拦截器重构 |
| `src/shared/api/index.ts` | 导出 ApiError、isAuthError、isHandledError |
| `src/main-desktop.ts` | onError 只显示 msg，不显示 code |
| `src/main-mobile.ts` | onError 只显示 msg，不显示 code |
| `src/mobile/views/Board.vue` | 隐私模式实现、AI+ 浮动按钮 |

---

## 技术要点

### 1. 错误处理分层

```
响应返回
  ├── code === 0 → 返回响应（业务代码正常处理）
  ├── 认证错误 → reject ApiError(isAuthError=true)
  │              ├── 全局处理：跳转登录页
  │              └── 业务代码：可用 isAuthError() 判断，忽略显示
  └── 业务错误 → reject ApiError(handled=true)
                 ├── 全局处理：显示错误提示
                 └── 业务代码：可选择是否额外处理
```

### 2. 移动端隐私模式与桌面端差异

| 功能 | 桌面端 | 移动端 |
|------|--------|--------|
| 账户按钮 | 可点击（弹窗内金额也隐藏） | 禁用点击 |
| 图表按钮 | 显示蒙版遮罩 | 禁用点击 |
| 原因 | 桌面端空间大，可显示蒙版 | 移动端交互简化 |

### 3. 浮动按钮规范

移动端浮动按钮统一规范：
- 尺寸：56x56px
- 圆角：50%（圆形）
- 位置：`right: 20px; bottom: 100px`
- 点击效果：`transform: scale(0.95)`

---

## 下午开发内容

### 4. 设置页面结构重构

#### 4.1 需求背景

Version API 变更，返回结构从单一 `VersionInfo` 扩展为：

```typescript
interface SystemConfig {
  versions: VersionInfo
  auth: AuthConfig      // 新增
  backup: BackupConfig  // 新增
}
```

需要合理展示认证信息、备份信息，同时优化设置页面结构。

#### 4.2 页面结构调整

**变更前：**
```
设置
├── 数据管理
│   ├── 收支管理
│   ├── 账户管理
│   ├── 分类管理
│   ├── 快记模板
│   └── AI+ 设置
├── 外观
│   └── 主题模式
└── 其他
    ├── 关于（版本可展开详情）
    └── 退出登录
```

**变更后：**
```
设置
├── 数据管理
│   ├── 收支管理
│   ├── 账户管理
│   ├── 分类管理
│   └── 快记模板
├── 系统管理          ← 新增
│   ├── AI+ 设置
│   └── 系统信息      ← 新增
└── 其他
    ├── 关于（仅显示 release 版本）
    └── 退出登录
```

#### 4.3 系统信息内容

| 区块 | 内容 |
|------|------|
| 外观设置 | 浅色/深色/跟随系统 主题切换 |
| 认证信息 | 认证状态、登录模式、会话时长 |
| 备份信息 | 备份状态、Cron 表达式 |
| 版本信息 | 前端/后端/数据库/AI Agent/WebHook |

---

### 5. 桌面端实现

#### 5.1 新增组件

**`src/desktop/views/settings/SystemInfo.vue`：**

侧拉抽屉组件，包含四个卡片式区块。

```vue
<script setup lang="ts">
const props = defineProps<{
  visible: boolean
  versions: VersionInfo
  authConfig: AuthConfig | null
  backupConfig: BackupConfig | null
}>()
</script>
```

#### 5.2 主题选择器

```vue
<div class="theme-selector">
  <div
    v-for="opt in themeOptions"
    :key="opt.value"
    class="theme-option"
    :class="{ active: currentTheme === opt.value }"
    @click="currentTheme = opt.value"
  >
    <el-icon :size="20"><component :is="opt.icon" /></el-icon>
    <span>{{ opt.label }}</span>
  </div>
</div>
```

#### 5.3 认证信息格式化

```typescript
const sessionTimeText = computed(() => {
  const minutes = props.authConfig?.expiredMinutes ?? 0
  if (minutes >= 60) {
    const hours = Math.floor(minutes / 60)
    const mins = minutes % 60
    return mins > 0 ? `${hours} 小时 ${mins} 分钟` : `${hours} 小时`
  }
  return `${minutes} 分钟`
})
```

---

### 6. 移动端实现

#### 6.1 新增页面

**`src/mobile/views/setting/SystemInfo.vue`：**

独立页面（非弹窗），通过路由跳转访问。

#### 6.2 路由配置

```typescript
{
  path: '/setting/system',
  name: 'SettingSystem',
  component: () => import('./views/setting/SystemInfo.vue'),
  meta: { title: '系统信息' },
}
```

#### 6.3 页面入口

```vue
<van-cell
  title="系统信息"
  icon="setting-o"
  is-link
  to="/setting/system"
/>
```

---

### 7. 关于对话框简化

移除版本详情展开功能，只显示 release 版本号。

**变更前：**
```vue
<div class="about-version-tag" @click="showVersionDetail = !showVersionDetail">
  <span>Version {{ versions.release }}</span>
  <van-icon :name="showVersionDetail ? 'arrow-up' : 'arrow-down'" />
</div>
<div v-if="showVersionDetail" class="version-detail">
  <!-- 各模块版本 -->
</div>
```

**变更后：**
```vue
<div class="about-version-tag">
  <span class="version-label">Version</span>
  <span class="version-value">{{ versions.release }}</span>
</div>
```

---

## 下午文件变更清单

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/desktop/views/settings/SystemInfo.vue` | 桌面端系统信息抽屉组件 |
| `src/mobile/views/setting/SystemInfo.vue` | 移动端系统信息页面 |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/shared/api/home.ts` | 新增 AuthConfig、BackupConfig、SystemConfig 类型 |
| `src/shared/api/index.ts` | 导出新类型 |
| `src/desktop/views/settings/index.vue` | 页面结构重构、集成 SystemInfo |
| `src/mobile/views/Setting.vue` | 页面结构重构、简化关于弹窗 |
| `src/mobile/router.ts` | 新增 /setting/system 路由 |

---

## 技术要点补充

### 4. API 类型扩展

```typescript
// src/shared/api/home.ts
export interface AuthConfig {
  enable: boolean
  expiredMinutes: number
  singleLogin: boolean
}

export interface BackupConfig {
  cron: string
  valid: boolean
  description: string
}

export interface SystemConfig {
  versions: VersionInfo
  auth: AuthConfig
  backup: BackupConfig
}
```

### 5. 认证状态影响

当 `auth.enable === false` 时：
- 自动清除本地 token
- 隐藏退出登录按钮

```typescript
if (authConfig.value && !authConfig.value.enable) {
  localStorage.removeItem('token')
}

const showLogout = computed(() => authConfig.value?.enable !== false)
```

### 6. 桌面端 vs 移动端系统信息

| 特性 | 桌面端 | 移动端 |
|------|--------|--------|
| 展示方式 | 侧拉抽屉（el-drawer） | 独立页面 |
| 主题切换 | 三个卡片横向排列 | 三个卡片横向排列 |
| 返回方式 | 点击遮罩/X按钮关闭 | 左上角返回按钮 |
| 刷新功能 | 无 | 右上角刷新按钮 |
