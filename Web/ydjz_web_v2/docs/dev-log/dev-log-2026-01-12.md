# 开发日志 - 2026年1月12日

## 今日开发概述

**主要工作：** 实现 **负债账户与不计入收支的双向限制逻辑**，完成记账页面（FlowAdd/FlowEditor）的账户类型显示和限制功能，更新总览页面账户明细显示。

---

## 文件变更统计

```
src/shared/api/home.ts                                 | 修改（HomeAccount 新增 accountType）
src/mobile/views/flow/FlowAdd.vue                      | 修改（双向限制、账户类型显示）
src/desktop/components/flow/FlowEditor.vue             | 修改（双向限制逻辑）
src/desktop/views/Board.vue                            | 修改（账户明细显示类型）
src/mobile/views/Board.vue                             | 修改（账户明细显示类型）
5 files changed (0 new, 5 modified)
```

---

## 今日开发内容

### 1. 负债账户与不计入收支的双向限制逻辑

#### 1.1 业务规则

负债账户不支持"不计入金额"功能，因此需要双向限制：

**账户 → 收支 限制（选了负债账户后）：**

| 负债账户位置 | 禁用的收支类型 |
|-------------|--------------|
| 源账户 | 普通不计入 + 转出不计入 + 两边不计入 |
| 目标账户 | 转入不计入 + 两边不计入 |

**收支 → 账户 限制（选了不计入收支后）：**

| 收支类型 | 禁用的账户 |
|---------|----------|
| 普通不计入（收入/支出） | 所有负债账户 |
| 转出不计入 | 负债账户不能作为源账户 |
| 转入不计入 | 负债账户不能作为目标账户 |
| 两边不计入 | 负债账户不能作为源/目标账户 |

#### 1.2 核心函数实现

```typescript
/**
 * 判断账户是否应该被禁用（根据已选收支类型）
 */
function isAccountDisabled(account: Account, panelType: 1 | 2): boolean {
  // 如果账户不是负债，不禁用
  if (account.accountType !== AccountType.LIABILITY) return false

  // 如果没选收支，或者收支不是"不计入"类型，不禁用
  if (!selectedAction.value?.exempt) return false

  // 普通不计入收支（非转账）：禁用所有负债账户
  if (selectedAction.value.handle !== ActionHandle.TRANSFER) {
    return true
  }

  // 内部转账的不计入，根据 exemptMode 判断
  const mode = selectedAction.value.exemptMode ?? ExemptMode.NONE

  if (panelType === 1) {
    // 源账户面板：禁用负债如果是转出不计入或两边不计入
    return mode === ExemptMode.FROM_EXEMPT || mode === ExemptMode.BOTH_EXEMPT
  } else {
    // 目标账户面板：禁用负债如果是转入不计入或两边不计入
    return mode === ExemptMode.TO_EXEMPT || mode === ExemptMode.BOTH_EXEMPT
  }
}

/**
 * 判断收支是否应该被禁用（根据已选账户）
 */
function isActionDisabled(action: Action): boolean {
  // 如果收支不是"不计入"类型，不禁用
  if (!action.exempt) return false

  // 检查源账户和目标账户是否为负债
  const sourceIsLiability = selectedAccount.value?.accountType === AccountType.LIABILITY
  const targetIsLiability = selectedAccountTo.value?.accountType === AccountType.LIABILITY

  // 如果都不是负债，不禁用
  if (!sourceIsLiability && !targetIsLiability) return false

  // 普通不计入收支（非转账）：只要源账户是负债就禁用
  if (action.handle !== ActionHandle.TRANSFER) {
    return sourceIsLiability
  }

  // 内部转账的不计入
  const mode = action.exemptMode ?? ExemptMode.NONE

  if (sourceIsLiability) {
    if (mode === ExemptMode.FROM_EXEMPT || mode === ExemptMode.BOTH_EXEMPT) {
      return true
    }
  }

  if (targetIsLiability) {
    if (mode === ExemptMode.TO_EXEMPT || mode === ExemptMode.BOTH_EXEMPT) {
      return true
    }
  }

  return false
}
```

---

### 2. 移动端记账页面（FlowAdd.vue）

#### 2.1 收支类型选择优化

导入 `ActionHandle` 和 `ExemptMode`，添加 `getExemptText` 函数：

```typescript
import { actionApi, type Action, ActionHandle, ExemptMode } from '@shared/api/action'

function getExemptText(action: Action): string {
  if (!action.exempt) return ''
  if (action.handle !== ActionHandle.TRANSFER) return '不计入'
  switch (action.exemptMode) {
    case ExemptMode.FROM_EXEMPT: return '转出不计入'
    case ExemptMode.TO_EXEMPT: return '转入不计入'
    case ExemptMode.BOTH_EXEMPT: return '两边不计入'
    default: return '不计入'
  }
}
```

**收支选择器 UI 更新：**
- 显示具体的不计入模式文本（转出不计入/转入不计入/两边不计入）
- 禁用状态：半透明 + 显示禁用原因

#### 2.2 账户选择器优化

**新增布局结构：**

```vue
<div class="account-left">
  <div class="account-type-tag" :class="account.accountType === AccountType.LIABILITY ? 'liability' : 'asset'">
    {{ account.accountType === AccountType.LIABILITY ? '负债' : '资产' }}
  </div>
  <div class="sheet-item-info">
    <span class="sheet-item-name">{{ account.name }}</span>
    <span v-if="isAccountDisabled(account, accountSheetType)" class="disabled-reason">
      {{ getAccountDisabledReason(account, accountSheetType) }}
    </span>
  </div>
</div>
<div class="account-right">
  <span class="account-balance">¥{{ account.money }}</span>
  <span v-if="account.exemptMoney && parseFloat(account.exemptMoney) !== 0" class="account-exempt">
    不计入 ¥{{ account.exemptMoney }}
  </span>
</div>
```

**显示效果：**
```
┌─────────────────────────────────────────────┐
│ [资产] 招商银行              ¥10,000.00     │
│                           不计入 ¥2,000.00   │
├─────────────────────────────────────────────┤
│ [负债] 信用卡                ¥-5,000.00      │
│        负债账户不支持此收支类型               │
└─────────────────────────────────────────────┘
```

**选中状态颜色：**
- 资产账户：绿色（--color-income）
- 负债账户：红色（--color-expense）

#### 2.3 移除旧限制逻辑

删除 `transferableAccounts` 计算属性（之前用于过滤有不计入金额的账户），改为只使用双向限制逻辑。

---

### 3. 桌面端流水编辑器（FlowEditor.vue）

#### 3.1 添加双向限制函数

与移动端相同的 `isAccountDisabled`、`isActionDisabled`、`getAccountDisabledReason`、`getActionDisabledReason` 函数。

#### 3.2 收支选择器更新

使用 `el-tooltip` 显示禁用原因：

```vue
<el-tooltip
  v-for="action in exemptActions"
  :key="action.id"
  :content="getActionDisabledReason(action)"
  :disabled="!isActionDisabled(action)"
  placement="top"
>
  <div
    class="action-tab exempt"
    :class="{
      active: selectedAction?.id === action.id,
      [getActionClass(action.handle)]: true,
      disabled: isActionDisabled(action)
    }"
    @click="!isActionDisabled(action) && onSelectAction(action)"
  >
    {{ action.hname }}
  </div>
</el-tooltip>
```

#### 3.3 账户选择器更新

```vue
<el-tooltip
  v-for="account in accounts"
  :key="account.id"
  :content="getAccountDisabledReason(account, accountPanelType)"
  :disabled="!isAccountDisabled(account, accountPanelType)"
  placement="right"
>
  <div
    class="account-item"
    :class="{
      active: ...,
      liability: account.accountType === AccountType.LIABILITY,
      disabled: isAccountDisabled(account, accountPanelType)
    }"
    @click="!isAccountDisabled(account, accountPanelType) && onSelectAccount(account)"
  >
    <!-- 账户内容 -->
  </div>
</el-tooltip>
```

#### 3.4 禁用状态样式

```css
/* 禁用状态 */
.action-tab.disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.action-tab.disabled:hover {
  background: rgba(255, 255, 255, 0.15);
}

.account-item.disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.account-item.disabled:hover {
  transform: none;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}
```

---

### 4. 总览页面账户明细（Board.vue）

#### 4.1 API 接口更新

`src/shared/api/home.ts` - `HomeAccount` 新增 `accountType` 字段：

```typescript
export interface HomeAccount {
  id: number
  accountName: string
  accountAsset: string
  exemptAsset: string
  note: string
  percent: string
  /** 账户类型：0=资产，1=负债 */
  accountType?: number
}
```

#### 4.2 桌面端 Board.vue

账户明细列表添加资产/负债类型标签：

```vue
<div class="account-item" v-for="acc in accountList" :key="acc.id">
  <div class="account-type-tag" :class="acc.accountType === 1 ? 'liability' : 'asset'">
    {{ acc.accountType === 1 ? '负债' : '资产' }}
  </div>
  <div class="account-info">
    <div class="account-name">{{ acc.accountName }}</div>
    <div class="account-note" v-if="acc.note">{{ acc.note }}</div>
  </div>
  <div class="account-amount">
    <div class="account-asset">¥ {{ privacyAccount(acc.accountAsset) }}</div>
    <div class="account-real" v-if="acc.realAsset">净 ¥ {{ ... }}</div>
  </div>
</div>
```

#### 4.3 移动端 Board.vue

账户概览弹窗列表添加资产/负债类型标签（与桌面端类似）。

#### 4.4 样式

```css
.account-type-tag {
  font-size: 11px;
  padding: 4px 8px;
  border-radius: 6px;
  font-weight: 500;
  flex-shrink: 0;
}

.account-type-tag.asset {
  background: var(--color-income-bg);
  color: var(--color-income);
}

.account-type-tag.liability {
  background: var(--color-expense-bg);
  color: var(--color-expense);
}
```

---

## 文件变更清单

| 文件路径 | 修改内容 |
|---------|---------|
| `src/shared/api/home.ts` | HomeAccount 新增 accountType 字段 |
| `src/mobile/views/flow/FlowAdd.vue` | 双向限制逻辑、账户类型显示、选中颜色、不计入金额显示 |
| `src/desktop/components/flow/FlowEditor.vue` | 双向限制逻辑、禁用状态样式 |
| `src/desktop/views/Board.vue` | 账户明细显示资产/负债类型标签 |
| `src/mobile/views/Board.vue` | 账户概览弹窗显示资产/负债类型标签 |

---

## 技术要点

### 1. 双向限制的响应式

由于 `isAccountDisabled` 和 `isActionDisabled` 是普通函数而非计算属性，每次渲染时都会重新计算。这确保了：

- 选择账户后，收支列表立即更新禁用状态
- 选择收支后，账户列表立即更新禁用状态
- 更改选择后，之前的禁用状态自动解除

### 2. 删除旧的过滤逻辑

移除了 `transferableAccounts` 计算属性，不再根据账户的 `exemptMoney` 过滤账户列表。现在所有账户都会显示，只有负债账户在特定收支类型下会被禁用（但仍然可见）。

### 3. 禁用原因提示

- 桌面端：使用 `el-tooltip` 悬停显示
- 移动端：直接在列表项中显示文字

---

## 后端依赖

**注意**：以下功能需要后端 API 支持：

1. 首页接口 `/home/getHomeInfoV2/{year}` 需要返回 `accountType` 字段
2. 如果后端未返回 `accountType`，默认显示为"资产"

---

## 已完成功能总结

### v2.6.0 版本功能（账户类型 + 不计入金额）

| 模块 | 桌面端 | 移动端 |
|------|--------|--------|
| 账户管理 | ✅ | ✅ |
| 收支管理 | ✅ | ✅ |
| 记账页面 | ✅ | ✅ |
| 总览页面 | ✅ | ✅ |
| 双向限制逻辑 | ✅ | ✅ |

---

## 备注

本日完成了负债账户与不计入收支的双向限制逻辑，确保用户无法创建无效的记账组合。同时优化了账户选择器的显示，增加了账户类型标签和不计入金额的展示。
