# 开发日志 - 2026年1月2日

## 今日开发概述

完成 **移动端页面状态恢复** 功能，解决页面跳转后筛选条件丢失的问题。涉及 Analysis、Flow、AnalysisType 三个页面。

---

## 文件变更统计

```
src/shared/stores/analysisFilter.ts         | 新增（Analysis 页面状态 Store）
src/shared/stores/flowFilter.ts             | 新增（Flow 页面状态 Store）
src/shared/stores/analysisTypeFilter.ts     | 新增（AnalysisType 页面状态 Store）
src/mobile/views/Analysis.vue               | 修改（状态保存/恢复 + UI 修复）
src/mobile/views/Flow.vue                   | 修改（状态保存/恢复）
src/mobile/views/analysis/AnalysisType.vue  | 修改（状态保存/恢复 + 流程优化）
6 files changed
```

---

## 今日开发内容

### 1. 问题背景

移动端页面跳转后，筛选条件会丢失：

| 场景 | 问题 |
|------|------|
| 统计 → 分类统计 → 返回 | 月份筛选重置 |
| 明细 → FlowAdd → 返回 | 月份和筛选条件丢失 |
| 分类统计 → 流水 popup → FlowAdd → 返回 | popup 关闭，筛选重置 |

### 2. 解决方案

使用 **Pinia Store** 保存页面状态，配合 `sessionStorage` 标记页面来源。

#### 2.1 核心模式

```typescript
// 1. 创建状态 Store
export const useXxxFilterStore = defineStore('xxxFilter', () => {
  const state1 = ref(...)
  const state2 = ref(...)
  const initialized = ref(false)

  function save(state: {...}) {
    // 保存所有状态
    initialized.value = true
  }

  function reset() {
    // 重置所有状态
    initialized.value = false
  }

  return { state1, state2, initialized, save, reset }
})

// 2. 页面中使用
onMounted(() => {
  if (filterStore.initialized) {
    // 恢复状态
  } else {
    // 初始化
  }
})

onBeforeUnmount(() => {
  filterStore.save({...})
})
```

#### 2.2 跨页面来源判断

使用 `sessionStorage` 标记页面来源，避免 `router.replace` 产生多余历史记录：

```typescript
// 来源页面（Analysis.vue）
function toTypeDetail(typeId: number) {
  sessionStorage.setItem('analysisTypeFrom', 'analysis')
  router.push({ path: '/analysis/type', query: {...} })
}

// 目标页面（AnalysisType.vue）
onMounted(() => {
  const isFromAnalysis = sessionStorage.getItem('analysisTypeFrom') === 'analysis'
  sessionStorage.removeItem('analysisTypeFrom')  // 立即清除

  if (isFromAnalysis) {
    // 从统计进入，使用路由参数
    filterStore.reset()
    initDateRange()
  } else if (filterStore.initialized) {
    // 从 FlowAdd 返回，恢复 store 状态
  }
})
```

### 3. 各页面状态清单

#### 3.1 Analysis.vue

| 状态 | 类型 | 说明 |
|------|------|------|
| fastChoose | number | 快捷时间选项 |
| startDate | string | 开始日期 |
| endDate | string | 结束日期 |
| tabIndex | number | 收入/支出 Tab |
| combineSubType | boolean | 合并子分类 |
| showDisableAnalysisType | boolean | 显示全部分类 |

#### 3.2 Flow.vue

| 状态 | 类型 | 说明 |
|------|------|------|
| chooseMonth | string | 当前月份 |
| handleType | number | 筛选类型（全部/流入/流出/转账） |
| orderType | number | 排序类型（按时间/按金额） |

#### 3.3 AnalysisType.vue

| 状态 | 类型 | 说明 |
|------|------|------|
| selectedTypeId | number | 选中的分类 ID |
| selectedTypeName | string | 选中的分类名称 |
| fastChoose | string | 快捷时间选项 |
| startDate | string | 开始日期 |
| endDate | string | 结束日期 |
| showFlowPopup | boolean | 流水弹窗是否打开 |
| flowMonth | string | 流水弹窗的月份 |
| flowChooseHandle | number | 流水类型（收入/支出） |
| flowList | Flow[] | 流水列表（临时） |
| result | AnalysisTypeMonthResult | 查询结果 |

### 4. 特殊处理

#### 4.1 流水弹窗状态恢复

从 FlowAdd 返回时，如果 popup 是打开状态，需要重新请求流水数据（可能有修改）：

```typescript
if (filterStore.showFlowPopup && filterStore.flowMonth) {
  const match = filterStore.flowMonth.match(/(\d+)年(\d+)月/)
  if (match) {
    const year = parseInt(match[1])
    const month = parseInt(match[2])
    onMonthClick(year, month, filterStore.flowChooseHandle)
  }
}
```

#### 4.2 跳转时不关闭 popup

```typescript
function onFlowClick(flow: Flow) {
  // 不关闭 popup，保持状态以便返回时恢复
  router.push({ path: `/flow/edit/${flow.id}` })
}
```

### 5. UI 修复

#### 5.1 "暂无数据" 居中对齐

Analysis.vue 的分类网格使用 2 列布局，空状态需要跨列居中：

```css
.type-grid :deep(.van-empty) {
  grid-column: span 2;
  padding-top: 40px;
}
```

#### 5.2 Tab 区域顶部间距

```css
.tab-section {
  margin-top: 10px;
}
```

---

## 文件变更清单

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/shared/stores/analysisFilter.ts` | Analysis 页面状态 Store |
| `src/shared/stores/flowFilter.ts` | Flow 页面状态 Store |
| `src/shared/stores/analysisTypeFilter.ts` | AnalysisType 页面状态 Store |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/mobile/views/Analysis.vue` | 状态保存/恢复，跳转时设置 sessionStorage 标记 |
| `src/mobile/views/Flow.vue` | 状态保存/恢复 |
| `src/mobile/views/analysis/AnalysisType.vue` | 状态保存/恢复，sessionStorage 来源判断，popup 状态处理 |

---

## 技术要点

### 为什么不用 URL 参数标记来源？

最初尝试在 URL 中添加 `from=analysis` 参数，然后用 `router.replace` 删除。但发现：

1. `router.replace` 在 `onMounted` 中执行会产生额外的历史记录
2. 导致点击返回时需要点两次

改用 `sessionStorage` 后：
- 跳转前设置标记
- 目标页面读取并立即删除
- 不影响路由历史

### Pinia Store vs Vuex

本项目使用 Pinia 作为状态管理：
- 更简洁的 API
- 更好的 TypeScript 支持
- 无需 mutations，直接修改状态

---

## 当前开发进度

| 模块 | 状态 | 说明 |
|-----|------|------|
| Analysis 状态恢复 | ✅ 完成 | 月份和筛选条件保持 |
| Flow 状态恢复 | ✅ 完成 | 月份和筛选条件保持 |
| AnalysisType 状态恢复 | ✅ 完成 | 完整状态保持，含 popup |
| Screen 状态恢复 | ✅ 完成 | 筛选条件 + 滚动定位 |
| AI 停止生成 | ✅ 完成 | 移动端支持停止生成 |
| AI 工具跳转扩展 | ✅ 完成 | 支持 flow 详情跳转 |

---

## 下午开发内容

### 6. Store 架构修复

移动端专用的 filter store 错误放在了 `shared/stores/`，应该放在 `mobile/stores/`：

```
移动前：src/shared/stores/analysisFilter.ts
移动后：src/mobile/stores/analysisFilter.ts
```

涉及文件：
- `analysisFilter.ts`
- `flowFilter.ts`
- `analysisTypeFilter.ts`

### 7. Screen 页面状态恢复

新增 `screenFilter.ts` store，保存筛选页面的完整状态：

| 状态 | 类型 | 说明 |
|------|------|------|
| searchNote | string | 备注搜索 |
| fastChoose | number | 快捷时间 |
| startDate | string | 开始日期 |
| endDate | string | 结束日期 |
| singleMonth | boolean | 单月模式 |
| accountId | number | 账户 ID |
| handleType | number | 收支类型 |
| collectOnly | boolean | 仅收藏 |
| chooseActions | number[] | 选中行为 |
| chooseTypes | number[] | 选中分类 |
| lastClickedFlowId | number | 最后点击的流水 ID |

#### 7.1 滚动定位 + 高亮

从 FlowAdd 返回时，滚动到之前点击的流水并高亮闪烁：

```typescript
async function scrollToFlowAndHighlight(flowId: number) {
  await nextTick()
  const el = document.querySelector(`[data-flow-id="${flowId}"]`)
  if (el) {
    el.scrollIntoView({ behavior: 'smooth', block: 'center' })
    // 使用 scrollend 事件确保滚动完成后再高亮
    if ('onscrollend' in window) {
      window.addEventListener('scrollend', () => addHighlight(el), { once: true })
    } else {
      setTimeout(() => addHighlight(el), 400)
    }
  }
}
```

FlowItem 组件添加 `data-flow-id` 属性和高亮动画 CSS。

### 8. 金额格式化

Screen 页面金额超过 1 万时显示为 "x.xx万"：

```typescript
function formatAmount(amount: number | string): string {
  const num = typeof amount === 'string' ? parseFloat(amount) : amount
  if (Math.abs(num) >= 10000) {
    return (num / 10000).toFixed(2) + '万'
  }
  return num.toFixed(2)
}
```

点击金额时 toast 显示原金额。

分类详情弹窗添加切换按钮，支持压缩/完整金额切换。

### 9. 移动端 AI 页面功能增强

#### 9.1 停止生成功能

从桌面端 AIDrawer.vue 移植停止功能：

```typescript
const { stopConversation } = useChatService()
const stopping = ref(false)

async function handleStop() {
  if (stopping.value) return
  stopping.value = true
  try {
    await stopConversation()
  } finally {
    stopping.value = false
  }
}
```

#### 9.2 输入区布局调整

将图片按钮和发送按钮改为垂直排列，使用文字+图标：

```
┌─────────────────────────────┐
│ 输入框                       │ [图片]
│                              │ [发送]
└─────────────────────────────┘
```

按钮统一样式：56px 宽，32px 高，带左侧图标。

#### 9.3 附件预览区改造

使用 Vant 组件改造：
- `van-image` 替代原生 img，支持点击预览
- `van-icon name="clear"` 作为关闭按钮
- 移除 `overflow: hidden` 修复角标被裁剪问题

### 10. AI 工具点击跳转扩展

#### 10.1 新增可导航工具

```typescript
// toolHelper.ts
export const navigableTools = ['flows', 'get_flow', 'add_flow', 'update_flow']
```

#### 10.2 跳转类型

| 工具 | 跳转类型 | 目标页面 |
|------|---------|---------|
| flows | flows | /screen（筛选页） |
| get_flow | flow | /flow/edit/:id |
| add_flow | flow | /flow/edit/:id |
| update_flow | flow | /flow/edit/:id |

#### 10.3 流式生成时禁用点击

避免跳转导致 WebSocket 断开：

```typescript
function handleToolClick(msg: UnifiedMessage) {
  // 流式生成时不允许点击
  if (isStreaming.value) return
  // ...
}
```

工具卡片的 `navigable` 样式也添加 `!isStreaming` 条件。

---

## 文件变更清单（下午）

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/mobile/stores/screenFilter.ts` | Screen 页面状态 Store |

### 移动文件

| 原路径 | 新路径 |
|-------|-------|
| `src/shared/stores/analysisFilter.ts` | `src/mobile/stores/analysisFilter.ts` |
| `src/shared/stores/flowFilter.ts` | `src/mobile/stores/flowFilter.ts` |
| `src/shared/stores/analysisTypeFilter.ts` | `src/mobile/stores/analysisTypeFilter.ts` |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/mobile/views/AI.vue` | 停止功能、输入区布局、附件预览、工具跳转禁用 |
| `src/mobile/views/Screen.vue` | 状态恢复、滚动定位、金额格式化 |
| `src/mobile/components/FlowItem.vue` | data-flow-id 属性、高亮动画 |
| `src/mobile/views/flow/FlowAdd.vue` | 滚动位置重置、流水不存在判断 |
| `src/shared/services/chat/toolHelper.ts` | 新增工具跳转支持 |
| `src/shared/services/chat/index.ts` | 导出新类型 |
| `src/desktop/components/ai-plus/AIDrawer.vue` | 工具点击跳转、FlowEditor 集成 |
| `src/desktop/components/flow/FlowEditor.vue` | 流水不存在判断 |

### 11. 桌面端 AI 工具跳转

桌面端 AIDrawer 也需要支持工具点击跳转：

#### 11.1 FlowEditor 集成

```typescript
import FlowEditor from '@desktop/components/flow/FlowEditor.vue'

// FlowEditor 状态
const flowEditorVisible = ref(false)
const flowEditorId = ref<number | null>(null)
```

#### 11.2 跳转处理

```typescript
function navigateFromTool(msg: UnifiedMessage) {
  const navResult = getToolNavigationParams(msg)
  if (!navResult) return

  if (navResult.type === 'flows' && navResult.params) {
    // 跳转到筛选页面
    setScreenParams(navResult.params)
    router.push(`${navResult.route}&_t=${Date.now()}`)
  } else if (navResult.type === 'flow' && navResult.flowParams) {
    // 打开流水编辑器
    flowEditorId.value = navResult.flowParams.flowId
    flowEditorVisible.value = true
  }
}
```

#### 11.3 流式禁用

与移动端一致，流式生成时禁用工具点击。

### 12. 流水不存在判断

AI 记录的流水可能被用户删除，但聊天记录中的工具卡片仍可点击。需要在 FlowAdd/FlowEditor 中处理这种情况。

#### 12.1 移动端 FlowAdd.vue

```typescript
// 流水不存在（已删除）
const flowNotFound = ref(false)

async function loadFlowDetail() {
  try {
    const res = await flowApi.getById(flowId.value)
    const data = res.data.data

    // 流水不存在
    if (!data) {
      flowNotFound.value = true
      showToast('该流水已被删除')
      return
    }
    // ...
  } catch (err) {
    flowNotFound.value = true
    showToast('流水加载失败，可能已被删除')
  }
}
```

模板中禁用提交按钮：

```vue
<button
  class="submit-btn"
  :class="{ disabled: flowNotFound }"
  :disabled="flowNotFound"
>
  {{ flowNotFound ? '流水已删除' : (isEdit ? '保存修改' : '提交账单') }}
</button>
```

#### 12.2 桌面端 FlowEditor.vue

同样添加 `flowNotFound` 状态，在 `loadFlowDetail` 中检测，禁用提交按钮并隐藏删除按钮。

`resetForm` 中重置 `flowNotFound = false`，确保下次打开时状态正确。
