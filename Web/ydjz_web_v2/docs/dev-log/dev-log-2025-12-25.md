# 开发日志 - 2025年12月25日

## 今日开发概述

完成 **FlowEditor 多面板扩展设计**、**Vant Calendar 滚动日历集成**、**抽屉 Header 结构优化**、**Board 概览页标签样式优化** 等功能。

---

## 文件变更统计

```
src/desktop/components/flow/FlowEditor.vue | +1037 -434
src/desktop/views/Board.vue                | +91 行变化
docs/dev-log-2025-12-25.md                 | 新增
```

---

## 今日开发内容

### 1. FlowEditor 多面板抽屉扩展设计

将原有的弹窗选择器改为面板扩展模式，抽屉宽度动态计算。

#### 架构设计

```
┌──────────────────────────────────────────────────────────────┐
│  编辑账单                               [快记模板]  [×]      │ ← 一级 header（贯穿顶部）
├─────────────────────────┬────────────────────────────────────┤
│                         │  [←返回] 选择账户                  │ ← 二级 header（子面板内）
│                         ├────────────────────────────────────┤
│    主面板内容            │    子面板内容                      │
│    (白色背景)            │    (淡灰 #f7f8fa)                 │
├─────────────────────────┤                                    │
│  [删除] [取消] [提交]    │                                    │
└─────────────────────────┴────────────────────────────────────┘
```

#### 动态宽度计算

```typescript
const drawerSize = computed(() => {
  let width = 480 // 基础宽度（主编辑区）
  if (accountPanelVisible.value) width += 360
  if (typePanelVisible.value) width += 400
  if (datePanelVisible.value) width += 340
  if (templatePanelVisible.value) width += 400
  return `${width}px`
})
```

#### 面板互斥机制

```typescript
function closeAllPanels() {
  accountPanelVisible.value = false
  typePanelVisible.value = false
  datePanelVisible.value = false
  templatePanelVisible.value = false
}

function openAccountPanel(type: 1 | 2) {
  closeAllPanels()
  accountPanelType.value = type
  accountPanelVisible.value = true
}
```

---

### 2. 账户面板样式优化

参考 AccountManager 的列表样式。

#### 账户图标支持

```typescript
import alipayIcon from '@shared/assets/icons/alipay.svg'
import wechatIcon from '@shared/assets/icons/wechat.svg'
import housingFundIcon from '@shared/assets/icons/housing-fund.svg'
import cashIcon from '@shared/assets/icons/cash.svg'

function getAccountIcon(name: string | undefined): string | null {
  if (!name) return null
  if (name.includes('支付宝')) return alipayIcon
  if (name.includes('微信')) return wechatIcon
  if (name.includes('公积金')) return housingFundIcon
  if (name.includes('现金')) return cashIcon
  return null
}
```

#### 卡片样式

```css
.account-item {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px 18px;
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(8px);
  border-radius: 14px;
  border: 1.5px solid rgba(0, 0, 0, 0.04);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}
```

---

### 3. 分类面板样式优化

参考 TypeManager 的可展开结构。

#### 展开/收起功能

```typescript
const expandedTypeIds = ref<number[]>([])

function toggleTypeExpand(id: number) {
  const idx = expandedTypeIds.value.indexOf(id)
  if (idx >= 0) {
    expandedTypeIds.value.splice(idx, 1)
  } else {
    expandedTypeIds.value.push(id)
  }
}
```

#### 一级分类不可直接选择（有子分类时）

```vue
<el-button
  v-if="!parent.childrenTypes?.length"
  type="primary"
  text
  size="small"
  @click="onSelectType(parent)"
>
  选择
</el-button>
```

---

### 4. Vant Calendar 滚动日历集成

将自定义日期列表改为 Vant 官方 Calendar 组件。

#### 引入方式

```typescript
import { Calendar as VanCalendar } from 'vant'
import 'vant/es/calendar/style'
```

#### 使用配置

```vue
<VanCalendar
  :poppable="false"
  :show-confirm="false"
  :min-date="calendarMinDate"
  :max-date="calendarMaxDate"
  :default-date="calendarDefaultDate"
  color="#1890ff"
  @confirm="onSelectDate"
/>
```

#### 日期范围

```typescript
const calendarMinDate = computed(() => {
  const d = new Date()
  d.setFullYear(d.getFullYear() - 1)
  return d
})
const calendarMaxDate = computed(() => new Date())
```

---

### 5. 抽屉 Header 结构优化

#### 一级 Header（贯穿顶部）

```vue
<div class="main-header">
  <span class="main-title">{{ isEdit ? '编辑账单' : '新增账单' }}</span>
  <div class="header-actions">
    <el-button v-if="!isEdit" text @click="openTemplatePanel">快记模板</el-button>
    <el-button :icon="Close" text circle @click="onClose" />
  </div>
</div>
```

#### 二级 Header（子面板内）

```vue
<div class="sub-panel-header">
  <el-button text :icon="ArrowLeft" @click="accountPanelVisible = false">返回</el-button>
  <span class="sub-panel-title">选择账户</span>
</div>
```

#### 子面板背景色

```css
.sub-panel {
  background: #f7f8fa; /* 浅灰色，比主面板略暗 */
}

html.dark .sub-panel {
  background: #1f1f1f;
}
```

---

### 6. Board 概览页标签样式优化

将最高值标签和负数标签改为贴着文本显示。

#### 模板改造

```vue
<!-- 修改前 -->
<span class="stat-num income" :class="{ 'max-tag': isMaxIncome(item.month) }">
  +{{ formatAmount(item.income) }}
</span>

<!-- 修改后 -->
<span class="stat-num income">
  <span :class="{ 'tag-max': isMaxIncome(item.month) }">+{{ formatAmount(item.income) }}</span>
</span>
```

#### 样式

```css
/* 最高值标签 - 贴着文本 */
.stat-num .tag-max {
  display: inline-block;
  padding: 2px 6px;
  border-radius: 4px;
  color: #fff;
}

/* 负数结余 - 贴着文本 */
.stat-num .tag-negative {
  display: inline-block;
  border: 1px solid var(--color-expense);
  border-radius: 4px;
  padding: 1px 5px;
}
```

---

## 文件变更清单

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `src/desktop/components/flow/FlowEditor.vue` | 多面板扩展设计、Vant Calendar、Header 结构优化 |
| `src/desktop/views/Board.vue` | 标签样式优化（贴着文本） |

---

## 技术要点

### Vant Calendar 平铺模式

- `poppable=false`：非弹窗，直接嵌入页面
- `show-confirm=false`：点击即选中，无需确认按钮
- 支持滚动浏览多个月份

### 动态抽屉宽度

使用 `computed` 计算抽屉宽度，根据打开的面板数量动态调整。

### 面板过渡动画

```css
.slide-panel-enter-active,
.slide-panel-leave-active {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.slide-panel-enter-from,
.slide-panel-leave-to {
  opacity: 0;
  transform: translateX(30px);
}
```

---

## PC 端开发进度

| 页面 | 状态 | 说明 |
|-----|------|------|
| Auth 登录页 | ✅ 完成 | 登录/注册功能 |
| HomeLayout 导航 | ✅ 完成 | 顶部胶囊导航 + 毛玻璃 |
| Board 总览 | ✅ 完成 | 年度趋势图表、月度概览、标签优化 |
| Flow 明细 | ✅ 完成 | 列表、日历、多面板编辑抽屉 |
| Analysis 统计 | 🔲 待开发 | - |
| Setting 设置 | ✅ 完成 | 模块化拆分、样式统一 |

---

## 待开发功能

- 统计页面（Analysis.vue）
- 导出报表功能完善
